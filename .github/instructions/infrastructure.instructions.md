---
applyTo: "docker-compose.yml,.env.example,prometheus/**/*.yml,**/*.toml,**/*.json"
---

Keep local infrastructure reproducible and document host versus Docker connection addresses. Never commit credentials. Validate Compose and configuration changes before changing application code.
